export const payload: ((previousResult: Record<string, any>) => {
  name: string;
  arguments: Record<string, any>;
})[] = [];

export const description = "Google Calendar description";
export const name = "gcalendar";
export const url = "http://localhost:8080";
